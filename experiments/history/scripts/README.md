# Archived Scripts

These scripts are retained as experiment history, not active operator entrypoints.
Prefer the maintained CLI tools in `src/rlab` and the small active helpers in
`scripts/` for current work.

Archived here:

- old audit and probe scripts used to diagnose stable-retro and CUDA behavior
- old benchmark scripts used for local throughput checks
- one-off model distillation, checkpoint hashing, best-episode recording, and
  W&B report/dashboard/backfill helpers

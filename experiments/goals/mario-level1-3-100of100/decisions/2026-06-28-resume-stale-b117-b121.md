# Resume Stale B117-B121 Jobs

Date: `2026-06-28`

Context: the initial Level1-3 screen jobs B117-B121 were intentionally stopped
by the operator before they could finish the 5M-step screen. Treat those queue
jobs as interrupted/stale, not as completed evidence.

Operator instruction: when the operator says to resume this goal, relaunch the
same interrupted jobs from their checked-in specs before designing a replacement
batch.

Relaunch specs:

- `experiments/goals/mario-level1-3-100of100/specs/b117-b55post21-l13-screen.yaml`
- `experiments/goals/mario-level1-3-100of100/specs/b118-b55complete25-l13-screen.yaml`
- `experiments/goals/mario-level1-3-100of100/specs/b119-b55slowent-l13-screen.yaml`
- `experiments/goals/mario-level1-3-100of100/specs/b120-b46style-l13-screen.yaml`
- `experiments/goals/mario-level1-3-100of100/specs/b121-b55slowent-complete25-l13-screen.yaml`

Use the standard profileless queue-backed launch path on `rtx4090` / `beast-3`
with an immutable train image digest. If `rlab-train-image.json` is absent, use
the queue CLI's latest-successful image resolver and record that in the next
launch snapshot.

Do not relaunch these jobs until the operator explicitly says to resume.

## Readiness Snapshot

Last checked: `2026-06-28`.

- `rlab-queue status --goal mario-level1-3-100of100` shows
  `train_jobs: {"failed": 5}`, no active train jobs, and no active eval jobs.
- `rlab-fleet policy` confirms the screening lane is `rtx4090` with
  `max_workers=5` and `env_threads=4`.
- `rlab-fleet plan` currently wants zero deployments because there is no queue
  demand; it would remove the one idle managed `beast-3` container if reconciled.
- `rlab-train-image.json` is currently absent at repo root, so the relaunch path
  should use the queue CLI's latest-successful image resolver unless that file
  appears before resume.
- All five relaunch specs load successfully with
  `rlab.job_queue.load_spec_document`.

## Resume Runbook

Only run this section after the operator explicitly says to resume.

First re-check the live state:

```bash
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue status \
  --goal mario-level1-3-100of100
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-fleet policy
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-fleet plan
```

If `rlab-train-image.json` exists, enqueue with the digest ref file:

```bash
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b117-b55post21-l13-screen.yaml \
  --runtime-image-ref-file rlab-train-image.json
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b118-b55complete25-l13-screen.yaml \
  --runtime-image-ref-file rlab-train-image.json
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b119-b55slowent-l13-screen.yaml \
  --runtime-image-ref-file rlab-train-image.json
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b120-b46style-l13-screen.yaml \
  --runtime-image-ref-file rlab-train-image.json
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b121-b55slowent-complete25-l13-screen.yaml \
  --runtime-image-ref-file rlab-train-image.json
```

If `rlab-train-image.json` is still absent, enqueue without that flag so the
queue CLI resolves the latest successful immutable train image:

```bash
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b117-b55post21-l13-screen.yaml
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b118-b55complete25-l13-screen.yaml
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b119-b55slowent-l13-screen.yaml
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b120-b46style-l13-screen.yaml
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue enqueue-train \
  --spec-file experiments/goals/mario-level1-3-100of100/specs/b121-b55slowent-complete25-l13-screen.yaml
```

Then reconcile capacity:

```bash
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-fleet plan
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-fleet reconcile
UV_CACHE_DIR=.uv-cache uv run --frozen rlab-queue status \
  --goal mario-level1-3-100of100
```

Monitor these metrics first:

- `train/info/level_complete/from/0-2/rate`
- `train/info/level_complete/from/0-2/count`
- `train/info/level_complete/rate/min/last`
- `train/info/level_complete/rate/mean/last`
- `train/reward_share/done`
- `rollout/ep_rew_mean`
- `train/approx_kl`
- `train/clip_fraction`
- `train/explained_variance`

Promote nothing from these screen runs until the goal's confirmation protocol
and out-of-process eval evidence exist.

defaults:
- ../_goal@goal
- _self_
schema_version: 1
slug: b121-b55slowent-complete25-l13-screen
hypothesis: >-
  Combining the B118 clean-completion reward with the B119 slower entropy schedule should help
  Level1-3 keep exploring long enough to discover clears while making real clean completions more
  reinforcing once found. Seed 80 should beat the separate B118 and B119 arms on peak
  train/info/level_complete/from/0-2/rate or reach the strict 100/100 Level1-3 source-attempt window
  before the 5M-step cap.
parent_spec_slug: b118-b55complete25-l13-screen
seeds:
- 80
run_target: rtx4090
wandb_group: b121-l13-b55slowent-complete25-screen
wandb_tags:
- mario
- level1-3
- post21
- b121
- b55-transfer
- completion-reward-25
- slow-entropy-decay
- higher-entropy-floor
- low-update-pressure
- screen
- beast3
run_name_template: b121_l13_b55slowent_complete25_s{seed}_{utc}
run_description_template: Single-seed Beast3 screen for Level1-3 100/100, seed {seed}; B55/B83 low-KL late-LR-decay recipe with slower entropy decay, ent_coef_final=0.001, and clean completion_reward=25 in score mode. Primary metric is peak train/info/level_complete/rate/min/last, matching train/info/level_complete/from/0-2/rate once the source window is full; stop early when train/info/level_complete/rate/min/last > 0.99.
selection_gate:
  primary: max(train/info/level_complete/rate/min/last)
  threshold: 1.0
  tie_breakers:
  - max(train/info/level_complete/rate/mean/last)
  training_metrics: primary_optimization_signal
train_config:
  game: SuperMarioBros-Nes-v0
  state: Level1-3
  n_envs: 16
  env_threads: 4
  torch_num_threads: 1
  n_steps: 512
  batch_size: 512
  n_epochs: 10
  learning_rate: 0.00015
  learning_rate_final: 0.0001
  learning_rate_schedule_timesteps: 4000000
  ent_coef: 0.01
  ent_coef_final: 0.001
  ent_coef_schedule_timesteps: 4000000
  gamma: 0.9
  gae_lambda: 1.0
  clip_range: 0.15
  target_kl: 0.16
  vf_coef: 1.0
  adam_eps: 1.0e-08
  normalize_advantage: false
  reward_mode: score
  progress_reward_cap: 30.0
  progress_reward_scale: 1.0
  terminal_reward: 50
  time_penalty: 0.0
  death_penalty: 25
  completion_reward: 25
  reward_scale: 10
  score_progress_clipped: false
  no_progress_timeout_steps: 0
  no_progress_min_delta: 0
  action_set: simple
  frame_skip: 4
  max_pool_frames: false
  hud_crop_top: 32
  max_episode_steps: 4500
  info_events_json:
    life_loss:
    - lives
    - decrease
    level_change:
    - - levelHi
      - levelLo
    - change
  done_on_events: life_loss,level_change
  eval_freq: 0
  eval_episodes: 0
  checkpoint_freq: 500000
  early_stop_metric: train/info/level_complete/rate/min/last
  early_stop_threshold: 0.99
  early_stop_operator: '>'
  timesteps: 5000000
  wandb: true
  wandb_mode: online
  wandb_artifact_storage_uri: ${CHECKPOINT_BUCKET_URI}
